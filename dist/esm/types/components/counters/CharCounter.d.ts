/// <reference types="react" />
import { ESize } from '../../consts';
export declare const MbCharCounter: ({ inputSize, maxChars, counter, }: {
    inputSize: ESize;
    maxChars: number;
    counter: number;
}) => JSX.Element;
