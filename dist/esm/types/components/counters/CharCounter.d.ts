/// <reference types="react" />
import { ESize } from '../../consts';
declare const MbCharCounter: ({ inputSize, maxChars, counter, }: {
    inputSize: ESize;
    maxChars: number;
    counter: number;
}) => JSX.Element;
export default MbCharCounter;
