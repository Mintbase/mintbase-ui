export enum EType {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
}
export enum EState {
  ACTIVE = 'active',
  CAUTION = 'caution',
  DISABLED = 'disabled',
  LOADING = 'loading',
}

export enum ESize {
  EXTRA_SMALL = "extra-small",
  SMALL = 'small',
  MEDIUM = 'medium',
  BIG = 'big',
}

export enum ESmallCardType {
  PROFILE = 'profile',
  STORE = 'store',
}
