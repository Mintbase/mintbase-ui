export enum EType {
  PRIMARY = "primary",
  SECONDARY = "secondary",
}
export enum EState {
  ACTIVE = "active",
  CAUTION = "caution",
  DISABLED = "disabled",
  LOADING = "loading",
}

export enum ESize {
  SMALL = "small",
  MEDIUM = "medium",
  BIG = "big",
}
